name: wf-push

on:
  push
jobs:
  push1:
    #if: ${{default_input_flag == "true"}}
    env:
      input_flag: ${{secrets.DEFAULT_INPUT_FLAG}}
    name: flag_evaulation
    runs-on: ubuntu-latest
    steps:

      - run: |
          echo "##[set-output name=a;]$(echo "a")"
          echo "##[set-output name=b;]$(echo "b")"
        id: esito
      - run: |
          echo ${{steps.esito.outputs.a}} , ${{steps.esito.outputs.b}}
  
